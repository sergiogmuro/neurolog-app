{
  "name": "neurolog_app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:app": "cd app && npm run dev",
    "dev:api": "docker-compose up",
    "dev": "npm-run-all --parallel dev:app dev:api",
    "down:api": "docker-compose down"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
